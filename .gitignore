# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Large / local-only artifacts (data/* not data/ so the manifest can be re-included)
data/*
checkpoints/
wandb/
results/preds/
.judge_cache/

# Personal study notes (not part of the release)
learn/

# Secrets & misc
.env
*.log

# The manifest defines the benchmark (sizes, seeds, qid fingerprints) — versioned.
!data/manifest.json
